/*
Como pegar um item selecionado de SELECT.
$('#idade').val()

ou ainda:
$('#idade').find('option').eq(1);

Para selecionar um item ainda não selecionado:
$('#idade').find('option').eq(1).attr('selected', 'selected');
*/